# Installed packages — Vercel installs these from package.json
node_modules/

# Local secrets. NEVER commit a real API key: this repo is public.
.env
.env.local
.env*.local

# macOS clutter
.DS_Store
.vercel
.env*
